#include <bits/stdc++.h>
using namespace std;
//#pragma GCC optimize("O3")

class DSU {
    public:
    vector<int> parent;
    vector<int> rank;
    DSU(int x) {
        parent = vector<int>(x,0);
        rank = vector<int>(x, 0);
    }

    void union() {
        
    }
    


    
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    DSU ob1 = DSU();

    return 0;
}





