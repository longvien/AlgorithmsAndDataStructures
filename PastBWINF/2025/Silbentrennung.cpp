#include <bits/stdc++.h>
using namespace std;
//#pragma GCC optimize("O3")

namespace FastIO {
	inline void init() {
		ios_base::sync_with_stdio(false);
		cin.tie(nullptr);
	}
}

signed main() {
    FastIO::init();
    return 0;
}